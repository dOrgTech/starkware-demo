import React from 'react';
import { Grid, Typography } from '@material-ui/core';

export const Home = (): JSX.Element => {
	return (
		<Grid container justify="center">
			<Typography>Starkware Labs</Typography>
		</Grid>
	);
};
